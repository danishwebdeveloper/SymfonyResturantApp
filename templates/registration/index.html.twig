{% extends 'base.html.twig' %}


{% block body %}

    <h2>
        Registration Form
    </h2>
    <div class="container">

    {# handle errors #}
    {% for error in errors %}
        <div class="alert alert-danger">
          {{ error.message }}
        </div>
    {% endfor %}
        {{ form(regForm) }}
    </div>

{% endblock %}
